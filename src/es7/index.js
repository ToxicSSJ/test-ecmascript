// definición de arreglo de prueba
let numbers = [1, 2, 3, 7, 8]

// ES7 se incluye el numero 7 en el arreglo?
if(numbers.includes(7)) {
    console.log('Si se encuentra el numero 7')
} else {
    console.log('No se encuentra el valor 7')
}

// ES7 se incluye el numero 9 en el arreglo?
if(numbers.includes(9)) {
    console.log('Si se encuentra el numero 9')
} else {
    console.log('No se encuentra el valor 9')
}

// Operador ** ES7
let base = 4;
let exponent = 4;
let result = base ** exponent; // elevando 4 a la 4 @_@

console.log(result)